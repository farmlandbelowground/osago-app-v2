import { type UseSocialsGeneratorResult } from '../../hooks/useSocialsGenerator'
import { type InstagramData } from '../../types'

export interface Props {
  generated: InstagramData
  socials: UseSocialsGeneratorResult
}
